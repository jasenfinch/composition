# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
warnings_are_errors: true

r:
  - release
  - devel

apt_packages:
  - libopenbabel-dev

r_github_packages: jasenfinch/mzAnnotation